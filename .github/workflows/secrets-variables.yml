name: secrets and variables
on: workflow_dispatch
jobs:
  secrets:
    env:
      USERNAME: Admin
      PASSWORD: Admin123
    runs-on: ubuntu-latest
    steps:
      - name: login database
        run: echo "login database using password $PASSWORD and username $USERNAME"
      - name: dispaly database credentials
        run: echo "db password is $PASSWORD and username $USERNAME"  
